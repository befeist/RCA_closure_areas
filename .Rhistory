colors = "transparent")
print(map)
# Print the map
print(map)
# Wait for user input to continue
cat("Press Enter to display the next polygon, or type 'quit' to stop: ")
answer <- readline()
# Break the loop if the answer is 'quit'
if (tolower(answer) == "quit") {
break
}
}
}
lon_boundary
lat_boundary
display_polygons_one_by_one(unique_areas)
display_polygons_one_by_one <- function(sf_object) {
for (i in 1:nrow(sf_object)) {
# Extract the current polygon
current_polygon <- sf_object[i, ]
# Extracting boundary names
lat_boundary1_name <- current_polygon$lat_boundary1
lat_boundary2_name <- current_polygon$lat_boundary2
lon_boundary1_name <- current_polygon$lon_boundary1
lon_boundary2_name <- current_polygon$lon_boundary2
# Selecting latitude and longitude lines
selected_latitude_lines <- lat_boundary[lat_boundary$boundary_name == lat_boundary1_name, ] %>%
rbind(lat_boundary[lat_boundary$boundary_name == lat_boundary2_name, ])
selected_longitude_lines <- lon_boundary[lon_boundary$boundary_name == lon_boundary1_name, ] %>%
rbind(lon_boundary[lon_boundary$boundary_name == lon_boundary2_name, ])
# Get min and max latitude
coords <- do.call(rbind, lapply(selected_latitude_lines$geometry, function(g) {
st_coordinates(g)
}))
min_latitude <- min(coords[,2])
max_latitude <- max(coords[,2])
# Get min and max longitude
coords <- do.call(rbind, lapply(selected_longitude_lines$geometry, function(g) {
st_coordinates(g)
}))
min_longitude <- min(coords[,1])
max_longitude <- max(coords[,1])
# Create a leaflet map and add the current polygon
map <- leaflet() %>%
addTiles() %>%
addPolygons(data = current_polygon, color = "red", weight=1) %>%
addPolylines(data = selected_latitude_lines, color = "blue", weight=2) %>%
addPolylines(data = selected_longitude_lines, color = "orange", weight=2) %>%
fitBounds(lng1 = min_longitude, lat1 =  max_latitude,
lng2 = max_longitude, lat2 = min_latitude) %>%
addLegend(position = "topright",
title = paste(lat_boundary1_name,lat_boundary2_name,lon_boundary1_name,lon_boundary2_name,
" (",i,"/",nrow(sf_object),")"),
opacity = 0,
labels = "",
colors = "transparent")
print(map)
# Print the map
print(map)
# Wait for user input to continue
cat("Press Enter to display the next polygon, or type 'quit' to stop: ")
answer <- readline()
# Break the loop if the answer is 'quit'
if (tolower(answer) == "quit") {
break
}
}
}
lon_boundary
lat_boundary
display_polygons_one_by_one(unique_areas)
display_polygons_one_by_one <- function(sf_object) {
for (i in 1:nrow(sf_object)) {
# Extract the current polygon
current_polygon <- sf_object[i, ]
# Extracting boundary names
lat_boundary1_name <- current_polygon$lat_boundary1
lat_boundary2_name <- current_polygon$lat_boundary2
lon_boundary1_name <- current_polygon$lon_boundary1
lon_boundary2_name <- current_polygon$lon_boundary2
# Selecting latitude and longitude lines
selected_latitude_lines <- lat_boundary[lat_boundary$boundary_name == lat_boundary1_name, ] %>%
rbind(lat_boundary[lat_boundary$boundary_name == lat_boundary2_name, ])
selected_longitude_lines <- lon_boundary[lon_boundary$boundary_name == lon_boundary1_name, ] %>%
rbind(lon_boundary[lon_boundary$boundary_name == lon_boundary2_name, ])
# Get min and max latitude
coords <- do.call(rbind, lapply(current_polygon$geometry, function(g) {
st_coordinates(g)
}))
min_latitude <- min(coords[,2])
max_latitude <- max(coords[,2])
min_longitude <- min(coords[,1])
max_longitude <- max(coords[,1])
# # Get min and max longitude
# coords <- do.call(rbind, lapply(selected_longitude_lines$geometry, function(g) {
#   st_coordinates(g)
# }))
# min_longitude <- min(coords[,1])
# max_longitude <- max(coords[,1])
# Create a leaflet map and add the current polygon
map <- leaflet() %>%
addTiles() %>%
addPolygons(data = current_polygon, color = "red", weight=1) %>%
addPolylines(data = selected_latitude_lines, color = "blue", weight=2) %>%
addPolylines(data = selected_longitude_lines, color = "orange", weight=2) %>%
fitBounds(lng1 = min_longitude, lat1 =  max_latitude,
lng2 = max_longitude, lat2 = min_latitude) %>%
addLegend(position = "topright",
title = paste(lat_boundary1_name,lat_boundary2_name,lon_boundary1_name,lon_boundary2_name,
" (",i,"/",nrow(sf_object),")"),
opacity = 0,
labels = "",
colors = "transparent")
print(map)
# Print the map
print(map)
# Wait for user input to continue
cat("Press Enter to display the next polygon, or type 'quit' to stop: ")
answer <- readline()
# Break the loop if the answer is 'quit'
if (tolower(answer) == "quit") {
break
}
}
}
lon_boundary
lat_boundary
display_polygons_one_by_one(unique_areas)
current_polygon
coords <- do.call(rbind, lapply(current_polygon$geometry, function(g) {
st_coordinates(g)
}))
coords <- do.call(rbind, lapply(current_polygon$geometry, function(g) {
st_coordinates(g)
}))
coords
current_polygon
coords <- do.call(rbind, lapply(current_polygon$geom, function(g) {
st_coordinates(g)
}))
min_latitude <- min(coords[,2])
max_latitude <- max(coords[,2])
min_longitude <- min(coords[,1])
max_longitude <- max(coords[,1])
max_longitude
display_polygons_one_by_one <- function(sf_object) {
for (i in 1:nrow(sf_object)) {
# Extract the current polygon
current_polygon <- sf_object[i, ]
# Extracting boundary names
lat_boundary1_name <- current_polygon$lat_boundary1
lat_boundary2_name <- current_polygon$lat_boundary2
lon_boundary1_name <- current_polygon$lon_boundary1
lon_boundary2_name <- current_polygon$lon_boundary2
# Selecting latitude and longitude lines
selected_latitude_lines <- lat_boundary[lat_boundary$boundary_name == lat_boundary1_name, ] %>%
rbind(lat_boundary[lat_boundary$boundary_name == lat_boundary2_name, ])
selected_longitude_lines <- lon_boundary[lon_boundary$boundary_name == lon_boundary1_name, ] %>%
rbind(lon_boundary[lon_boundary$boundary_name == lon_boundary2_name, ])
# Get min and max latitude
coords <- do.call(rbind, lapply(current_polygon$geom, function(g) {
st_coordinates(g)
}))
min_latitude <- min(coords[,2])
max_latitude <- max(coords[,2])
min_longitude <- min(coords[,1])
max_longitude <- max(coords[,1])
# # Get min and max longitude
# coords <- do.call(rbind, lapply(selected_longitude_lines$geometry, function(g) {
#   st_coordinates(g)
# }))
# min_longitude <- min(coords[,1])
# max_longitude <- max(coords[,1])
# Create a leaflet map and add the current polygon
map <- leaflet() %>%
addTiles() %>%
addPolygons(data = current_polygon, color = "red", weight=1) %>%
addPolylines(data = selected_latitude_lines, color = "blue", weight=2) %>%
addPolylines(data = selected_longitude_lines, color = "orange", weight=2) %>%
fitBounds(lng1 = min_longitude, lat1 =  max_latitude,
lng2 = max_longitude, lat2 = min_latitude) %>%
addLegend(position = "topright",
title = paste(lat_boundary1_name,lat_boundary2_name,lon_boundary1_name,lon_boundary2_name,
" (",i,"/",nrow(sf_object),")"),
opacity = 0,
labels = "",
colors = "transparent")
print(map)
# Print the map
print(map)
# Wait for user input to continue
cat("Press Enter to display the next polygon, or type 'quit' to stop: ")
answer <- readline()
# Break the loop if the answer is 'quit'
if (tolower(answer) == "quit") {
break
}
}
}
lon_boundary
lat_boundary
display_polygons_one_by_one(unique_areas)
display_polygons_one_by_one <- function(sf_object) {
for (i in 1:nrow(sf_object)) {
# Extract the current polygon
current_polygon <- sf_object[i, ]
# Extracting boundary names
lat_boundary1_name <- current_polygon$lat_boundary1
lat_boundary2_name <- current_polygon$lat_boundary2
lon_boundary1_name <- current_polygon$lon_boundary1
lon_boundary2_name <- current_polygon$lon_boundary2
# Selecting latitude and longitude lines
selected_latitude_lines <- lat_boundary[lat_boundary$boundary_name == lat_boundary1_name, ] %>%
rbind(lat_boundary[lat_boundary$boundary_name == lat_boundary2_name, ])
selected_longitude_lines <- lon_boundary[lon_boundary$boundary_name == lon_boundary1_name, ] %>%
rbind(lon_boundary[lon_boundary$boundary_name == lon_boundary2_name, ])
# Get min and max latitude
coords <- do.call(rbind, lapply(current_polygon$geom, function(g) {
st_coordinates(g)
}))
min_latitude <- min(coords[,2])
max_latitude <- max(coords[,2])
min_longitude <- min(coords[,1])
max_longitude <- max(coords[,1])
# Create a leaflet map and add the current polygon
map <- leaflet() %>%
addTiles() %>%
addPolygons(data = current_polygon, color = "red", weight=1) %>%
addPolylines(data = selected_latitude_lines, color = "blue", weight=2) %>%
addPolylines(data = selected_longitude_lines, color = "orange", weight=2) %>%
fitBounds(lng1 = min_longitude, lat1 =  max_latitude,
lng2 = max_longitude, lat2 = min_latitude) %>%
addLegend(position = "topright",
title = paste(lat_boundary1_name,"\n",
lat_boundary2_name,"\n",
lon_boundary1_name,"\n",
lon_boundary2_name,"\n",
"\n("," (",i,"/",nrow(sf_object),")"),
opacity = 0,
labels = "",
colors = "transparent")
print(map)
# Print the map
print(map)
# Wait for user input to continue
cat("Press Enter to display the next polygon, or type 'quit' to stop: ")
answer <- readline()
# Break the loop if the answer is 'quit'
if (tolower(answer) == "quit") {
break
}
}
}
lon_boundary
lat_boundary
display_polygons_one_by_one(unique_areas)
leaflet() %>%
addTiles() %>%
addPolygons(data = current_polygon, color = "red", weight=1) %>%
addPolylines(data = selected_latitude_lines, color = "blue", weight=2) %>%
addPolylines(data = selected_longitude_lines, color = "orange", weight=2) %>%
fitBounds(lng1 = min_longitude, lat1 =  max_latitude,
lng2 = max_longitude, lat2 = min_latitude) %>%
addLegend(position = "topright",
title = paste(lat_boundary1_name,"<br>",
lat_boundary2_name,"<br>",
lon_boundary1_name,"<br>",
lon_boundary2_name,"<br>",
"<br>"," (",i,"/",nrow(sf_object),")"),
opacity = 0,
labels = "",
colors = "transparent")
display_polygons_one_by_one <- function(sf_object) {
for (i in 1:nrow(sf_object)) {
# Extract the current polygon
current_polygon <- sf_object[i, ]
# Extracting boundary names
lat_boundary1_name <- current_polygon$lat_boundary1
lat_boundary2_name <- current_polygon$lat_boundary2
lon_boundary1_name <- current_polygon$lon_boundary1
lon_boundary2_name <- current_polygon$lon_boundary2
# Selecting latitude and longitude lines
selected_latitude_lines <- lat_boundary[lat_boundary$boundary_name == lat_boundary1_name, ] %>%
rbind(lat_boundary[lat_boundary$boundary_name == lat_boundary2_name, ])
selected_longitude_lines <- lon_boundary[lon_boundary$boundary_name == lon_boundary1_name, ] %>%
rbind(lon_boundary[lon_boundary$boundary_name == lon_boundary2_name, ])
# Get min and max latitude
coords <- do.call(rbind, lapply(current_polygon$geom, function(g) {
st_coordinates(g)
}))
min_latitude <- min(coords[,2])
max_latitude <- max(coords[,2])
min_longitude <- min(coords[,1])
max_longitude <- max(coords[,1])
# Create a leaflet map and add the current polygon
map <- leaflet() %>%
addTiles() %>%
addPolygons(data = current_polygon, color = "red", weight=1) %>%
addPolylines(data = selected_latitude_lines, color = "blue", weight=2) %>%
addPolylines(data = selected_longitude_lines, color = "orange", weight=2) %>%
fitBounds(lng1 = min_longitude, lat1 =  max_latitude,
lng2 = max_longitude, lat2 = min_latitude) %>%
addLegend(position = "topright",
title = paste(lat_boundary1_name,"<br>",
lat_boundary2_name,"<br>",
lon_boundary1_name,"<br>",
lon_boundary2_name,"<br>",
"<br>"," (",i,"/",nrow(sf_object),")"),
opacity = 0,
labels = "",
colors = "transparent")
print(map)
# Print the map
print(map)
# Wait for user input to continue
cat("Press Enter to display the next polygon, or type 'quit' to stop: ")
answer <- readline()
# Break the loop if the answer is 'quit'
if (tolower(answer) == "quit") {
break
}
}
}
lon_boundary
lat_boundary
display_polygons_one_by_one(unique_areas)
display_polygons_one_by_one <- function(sf_object) {
for (i in 1:nrow(sf_object)) {
# Extract the current polygon
current_polygon <- sf_object[i, ]
# Extracting boundary names
lat_boundary1_name <- current_polygon$lat_boundary1
lat_boundary2_name <- current_polygon$lat_boundary2
lon_boundary1_name <- current_polygon$lon_boundary1
lon_boundary2_name <- current_polygon$lon_boundary2
# Selecting latitude and longitude lines
selected_latitude_lines <- lat_boundary[lat_boundary$boundary_name == lat_boundary1_name, ] %>%
rbind(lat_boundary[lat_boundary$boundary_name == lat_boundary2_name, ])
selected_longitude_lines <- lon_boundary[lon_boundary$boundary_name == lon_boundary1_name, ] %>%
rbind(lon_boundary[lon_boundary$boundary_name == lon_boundary2_name, ])
# Get min and max latitude
coords <- do.call(rbind, lapply(current_polygon$geom, function(g) {
st_coordinates(g)
}))
min_latitude <- min(coords[,2])
max_latitude <- max(coords[,2])
min_longitude <- min(coords[,1])
max_longitude <- max(coords[,1])
# Create a leaflet map and add the current polygon
map <- leaflet() %>%
addTiles() %>%
addPolygons(data = current_polygon, color = "red", weight=1) %>%
addPolylines(data = selected_latitude_lines, color = "blue", weight=2) %>%
addPolylines(data = selected_longitude_lines, color = "orange", weight=2) %>%
fitBounds(lng1 = min_longitude, lat1 =  max_latitude,
lng2 = max_longitude, lat2 = min_latitude) %>%
addLegend(position = "topright",
title = paste(lat_boundary1_name,"<br>",
lat_boundary2_name,"<br>",
lon_boundary1_name,"<br>",
lon_boundary2_name,"<br>",
" (",i,"/",nrow(sf_object),")"),
opacity = 0,
labels = "",
colors = "transparent")
print(map)
# Print the map
print(map)
# Wait for user input to continue
cat("Press Enter to display the next polygon, or type 'quit' to stop: ")
answer <- readline()
# Break the loop if the answer is 'quit'
if (tolower(answer) == "quit") {
break
}
}
}
lon_boundary
lat_boundary
display_polygons_one_by_one(unique_areas)
unique_areas
current_polygon <- sf_object[136, ]
unique_areas[136, ]
unique_areas[137, ]
current_polygon <- sf_object[137, ]
current_polygon <- unique_areas[137, ]
lat_boundary1_name <- current_polygon$lat_boundary1
lat_boundary1_name
coords <- do.call(rbind, lapply(current_polygon$geom, function(g) {
st_coordinates(g)
}))
library(sf)
library(leaflet)
library(dplyr)
display_polygons_one_by_one <- function(sf_object) {
for (i in 1:nrow(sf_object)) {
tryCatch({
# Extract the current polygon
current_polygon <- sf_object[i, ]
# Assuming lat_boundary and lon_boundary are predefined datasets
# Extracting boundary names
lat_boundary1_name <- current_polygon$lat_boundary1
lat_boundary2_name <- current_polygon$lat_boundary2
lon_boundary1_name <- current_polygon$lon_boundary1
lon_boundary2_name <- current_polygon$lon_boundary2
# Selecting latitude and longitude lines
selected_latitude_lines <- lat_boundary[lat_boundary$boundary_name == lat_boundary1_name, ] %>%
rbind(lat_boundary[lat_boundary$boundary_name == lat_boundary2_name, ])
selected_longitude_lines <- lon_boundary[lon_boundary$boundary_name == lon_boundary1_name, ] %>%
rbind(lon_boundary[lon_boundary$boundary_name == lon_boundary2_name, ])
# Get min and max latitude and longitude
coords <- do.call(rbind, lapply(current_polygon$geometry, function(g) {
st_coordinates(g)
}))
min_latitude <- min(coords[,2])
max_latitude <- max(coords[,2])
min_longitude <- min(coords[,1])
max_longitude <- max(coords[,1])
# Create a leaflet map and add the current polygon
map <- leaflet() %>%
addTiles() %>%
addPolygons(data = current_polygon, color = "red", weight=1) %>%
addPolylines(data = selected_latitude_lines, color = "blue", weight=2) %>%
addPolylines(data = selected_longitude_lines, color = "orange", weight=2) %>%
fitBounds(lng1 = min_longitude, lat1 =  max_latitude,
lng2 = max_longitude, lat2 = min_latitude) %>%
addLegend(position = "topright",
title = paste(lat_boundary1_name,"<br>",
lat_boundary2_name,"<br>",
lon_boundary1_name,"<br>",
lon_boundary2_name,"<br>",
" (",i,"/",nrow(sf_object),")"),
opacity = 0,
labels = "",
colors = "transparent")
# Print the map
print(map)
# Wait for user input to continue
cat("Press Enter to display the next polygon, or type 'quit' to stop: ")
answer <- readline()
# Break the loop if the answer is 'quit'
if (tolower(answer) == "quit") {
break
}
}, error = function(e) {
# Error handling: print the error message and continue with the next iteration
print(paste("An error occurred: ", e$message, "Continuing to the next item."))
})
}
}
display_polygons_one_by_one(unique_areas[136:140])
display_polygons_one_by_one(unique_areas[135:140])
display_polygons_one_by_one(unique_areas[135:140,])
display_polygons_one_by_one(unique_areas)
empty_geoms <- st_is_empty(unique_areas)
unique_areas_non_empty <- unique_areas[!empty_geoms, ]
dim(unique_areas_non_empty)
display_polygons_one_by_one(unique_areas_non_empty)
display_polygons_one_by_one <- function(sf_object) {
for (i in 1:nrow(sf_object)) {
# Extract the current polygon
current_polygon <- sf_object[i, ]
# Extracting boundary names
lat_boundary1_name <- current_polygon$lat_boundary1
lat_boundary2_name <- current_polygon$lat_boundary2
lon_boundary1_name <- current_polygon$lon_boundary1
lon_boundary2_name <- current_polygon$lon_boundary2
# Selecting latitude and longitude lines
selected_latitude_lines <- lat_boundary[lat_boundary$boundary_name == lat_boundary1_name, ] %>%
rbind(lat_boundary[lat_boundary$boundary_name == lat_boundary2_name, ])
selected_longitude_lines <- lon_boundary[lon_boundary$boundary_name == lon_boundary1_name, ] %>%
rbind(lon_boundary[lon_boundary$boundary_name == lon_boundary2_name, ])
# Get min and max latitude
coords <- do.call(rbind, lapply(current_polygon$geom, function(g) {
st_coordinates(g)
}))
min_latitude <- min(coords[,2])
max_latitude <- max(coords[,2])
min_longitude <- min(coords[,1])
max_longitude <- max(coords[,1])
# Create a leaflet map and add the current polygon
map <- leaflet() %>%
addTiles() %>%
addPolygons(data = current_polygon, color = "red", weight=1) %>%
addPolylines(data = selected_latitude_lines, color = "blue", weight=2) %>%
addPolylines(data = selected_longitude_lines, color = "orange", weight=2) %>%
fitBounds(lng1 = min_longitude, lat1 =  max_latitude,
lng2 = max_longitude, lat2 = min_latitude) %>%
addLegend(position = "topright",
title = paste(lat_boundary1_name,"<br>",
lat_boundary2_name,"<br>",
lon_boundary1_name,"<br>",
lon_boundary2_name,"<br>",
" (",i,"/",nrow(sf_object),")"),
opacity = 0,
labels = "",
colors = "transparent")
print(map)
# Print the map
print(map)
# Wait for user input to continue
cat("Press Enter to display the next polygon, or type 'quit' to stop: ")
answer <- readline()
# Break the loop if the answer is 'quit'
if (tolower(answer) == "quit") {
break
}
}
}
empty_geoms <- st_is_empty(unique_areas)
unique_areas_non_empty <- unique_areas[!empty_geoms, ]
display_polygons_one_by_one(unique_areas_non_empty)
